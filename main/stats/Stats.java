package stats;

//import org.apache.commons.math.stat.StatUtils;
import file.FileReader;

public class Stats
{
	public void generateStats()
	{
/*		FileReader getDataFiles = new FileReader();
		String[] listOfFiles = null;
		
		listOfFiles = getDataFiles.getListOfFiles();
		parseData(listOfFiles);
*/
	}
	
	private void parseDate(String[] listOfFiles)
	{
	}
	
	
/* modif code:
1) sortie html
    a) 3 stats: valeur min, max, moyenne, l`écart type, 25% pourcentile, median, 75% pourcentile
    b) temps d`une éxecution
    c) le nombre d`éxecution par jour
    d) le temps de turn-around (rouge à vert)
	
	*/
}